import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.chapuytime.nimbus',
  appName: 'Nimbus',
  webDir: 'dist/chapuy-time/browser'
};

export default config;
